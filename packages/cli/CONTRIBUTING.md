# Contributing

Use Node.js 22 or 24 and the pnpm version pinned by the S11tnext repository.

Before submitting a change, run `pnpm verify` and the Phase 5 package checks from the repository root.

Keep filesystem and TOML handling in the CLI. Public contract changes must include fixtures and deterministic tests.
